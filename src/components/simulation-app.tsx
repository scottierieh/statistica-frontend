
'use client';

import GradientDescentPage from "./pages/gradient-descent-page";

export default function SimulationApp() {
  // Currently, only Gradient Descent is implemented.
  // This component can be expanded to include other simulation types.
  return <GradientDescentPage />;
}
