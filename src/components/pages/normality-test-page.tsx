'use client';

import React from 'react';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function NormalityTestPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Normality Test</CardTitle>
        <CardDescription>
          This component is not yet implemented.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>This page will allow you to test for normality in your data.</p>
      </CardContent>
    </Card>
  );
}
