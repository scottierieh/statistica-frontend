'use client';

import React from 'react';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function SnaPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Social Network Analysis</CardTitle>
        <CardDescription>
          This component is not yet implemented.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>This page will allow you to perform Social Network Analysis.</p>
      </CardContent>
    </Card>
  );
}
