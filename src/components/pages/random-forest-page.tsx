'use client';

import React from 'react';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function RandomForestPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Random Forest</CardTitle>
        <CardDescription>
          This component is not yet implemented.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>This page will allow you to build Random Forest models.</p>
      </CardContent>
    </Card>
  );
}
