'use client';

import React from 'react';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function ScenarioSensitivityPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Scenario Sensitivity Analysis</CardTitle>
        <CardDescription>
          This component is under construction.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>This page will allow you to perform scenario sensitivity analysis.</p>
      </CardContent>
    </Card>
  );
}
