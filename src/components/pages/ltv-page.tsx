'use client';

import React from 'react';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function LtvPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>LTV Prediction</CardTitle>
        <CardDescription>
          This component is not yet implemented.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>This page will allow you to predict Customer Lifetime Value.</p>
      </CardContent>
    </Card>
  );
}
