'use client';

import React from 'react';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function OutlierDetectionPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Outlier Detection</CardTitle>
        <CardDescription>
          This component is not yet implemented.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>This page will allow you to detect outliers in your data.</p>
      </CardContent>
    </Card>
  );
}
