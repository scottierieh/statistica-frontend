'use client';

import React from 'react';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function RelativeImportancePage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Relative Importance Analysis</CardTitle>
        <CardDescription>
          This component is not yet implemented.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>This page will allow you to analyze the relative importance of predictors.</p>
      </CardContent>
    </Card>
  );
}
