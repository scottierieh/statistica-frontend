'use client';

import React from 'react';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function IndependentSamplesTTestPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Independent Samples T-Test</CardTitle>
        <CardDescription>
          This component is not yet implemented.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>This page will allow you to perform an independent samples t-test.</p>
      </CardContent>
    </Card>
  );
}
