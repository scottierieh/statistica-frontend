'use client';

import React from 'react';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function GlmPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Generalized Linear Model (GLM)</CardTitle>
        <CardDescription>
          This component is not yet implemented.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>This page will allow you to perform Generalized Linear Modeling.</p>
      </CardContent>
    </Card>
  );
}
