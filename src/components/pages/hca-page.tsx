'use client';

import React from 'react';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function HcaPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Hierarchical Clustering Analysis (HCA)</CardTitle>
        <CardDescription>
          This component is not yet implemented.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>This page will allow you to perform Hierarchical Clustering Analysis.</p>
      </CardContent>
    </Card>
  );
}
