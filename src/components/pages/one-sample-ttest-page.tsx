'use client';

import React from 'react';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function OneSampleTTestPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>One-Sample T-Test</CardTitle>
        <CardDescription>
          This component is not yet implemented.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>This page will allow you to perform a one-sample t-test.</p>
      </CardContent>
    </Card>
  );
}
