'use client';

import React from 'react';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function AnovaPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>One-Way ANOVA</CardTitle>
        <CardDescription>
          This component is not yet implemented.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>This page will allow you to perform a one-way ANOVA.</p>
      </CardContent>
    </Card>
  );
}
