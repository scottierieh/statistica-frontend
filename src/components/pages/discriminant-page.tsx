'use client';

import React from 'react';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function DiscriminantPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Linear Discriminant Analysis (LDA)</CardTitle>
        <CardDescription>
          This component is not yet implemented.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>This page will allow you to perform Linear Discriminant Analysis.</p>
      </CardContent>
    </Card>
  );
}
