'use client';

import React from 'react';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function CrosstabPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Crosstab & Chi-Squared</CardTitle>
        <CardDescription>
          This component is not yet implemented.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>This page will allow you to perform crosstabulation and Chi-Squared tests.</p>
      </CardContent>
    </Card>
  );
}
