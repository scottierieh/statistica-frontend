'use client';

import React from 'react';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function KMedoidsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>K-Medoids Clustering</CardTitle>
        <CardDescription>
          This component is not yet implemented.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>This page will allow you to perform K-Medoids clustering.</p>
      </CardContent>
    </Card>
  );
}
