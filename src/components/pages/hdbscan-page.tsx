'use client';

import React from 'react';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function HdbscanPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>HDBSCAN Clustering</CardTitle>
        <CardDescription>
          This component is not yet implemented.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>This page will allow you to perform HDBSCAN clustering.</p>
      </CardContent>
    </Card>
  );
}
