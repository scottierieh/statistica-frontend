'use client';

import React from 'react';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function FriedmanPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Friedman Test</CardTitle>
        <CardDescription>
          This component is not yet implemented.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>This page will allow you to perform a Friedman test.</p>
      </CardContent>
    </Card>
  );
}
