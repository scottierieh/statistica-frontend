'use client';

import React from 'react';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function KruskalPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Kruskal-Wallis H Test</CardTitle>
        <CardDescription>
          This component is not yet implemented.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>This page will allow you to perform a Kruskal-Wallis H test.</p>
      </CardContent>
    </Card>
  );
}
