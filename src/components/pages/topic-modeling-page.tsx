'use client';

import React from 'react';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function TopicModelingPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Topic Modeling (LDA)</CardTitle>
        <CardDescription>
          This component is under construction.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>This page will allow you to perform topic modeling on your text data.</p>
      </CardContent>
    </Card>
  );
}
