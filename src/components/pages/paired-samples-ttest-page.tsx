'use client';

import React from 'react';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function PairedSamplesTTestPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Paired Samples T-Test</CardTitle>
        <CardDescription>
          This component is not yet implemented.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>This page will allow you to perform a paired samples t-test.</p>
      </CardContent>
    </Card>
  );
}
