'use client';

import React from 'react';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function WelchsTTestPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Welch's T-test</CardTitle>
        <CardDescription>
          This component is not yet implemented.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>This page will allow you to perform Welch's t-test.</p>
      </CardContent>
    </Card>
  );
}
