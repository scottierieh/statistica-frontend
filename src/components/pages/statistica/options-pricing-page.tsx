'use client';

import React from 'react';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function OptionsPricingPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Options Pricing</CardTitle>
        <CardDescription>
          This component is not yet implemented.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>This page will allow you to price options using various models.</p>
      </CardContent>
    </Card>
  );
}
