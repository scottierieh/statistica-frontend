'use client';

import React from 'react';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function WilcoxonPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Wilcoxon Signed-Rank Test</CardTitle>
        <CardDescription>
          This component is not yet implemented.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>This page will allow you to perform a Wilcoxon signed-rank test.</p>
      </CardContent>
    </Card>
  );
}
