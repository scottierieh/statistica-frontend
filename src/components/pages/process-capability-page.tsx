'use client';

import React from 'react';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function ProcessCapabilityPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Process Capability</CardTitle>
        <CardDescription>
          This component is not yet implemented.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>This page will allow you to analyze process capability (Cp, Cpk).</p>
      </CardContent>
    </Card>
  );
}
